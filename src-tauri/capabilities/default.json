{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口与设置窗口的 IPC 权限",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
